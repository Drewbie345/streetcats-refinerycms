<%= render 'refinery/admin/search_header', :url => refinery.cats_admin_cats_path %>
<div class='pagination_container'>
  <% if @cats.any? %>
    <%= render 'cats' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
